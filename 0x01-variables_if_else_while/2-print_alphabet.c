#include <stdio.h>

/**
 * main - displaying lowercase alphabet
 *
 * return: Always 0 (success)
 */

int main(void)
{	
	for(char ch = 'a'; ch < 'z'; ch++)
	{
		putchar(ch);
		
        }
		putchar('\n');
	 return (0);
}
